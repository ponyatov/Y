%{
#include "sym.hpp"
%}

%defines

%union {
	sym *s;
}

%token <s> S MODULE

%%
sym :
	| sym MODULE S	{ W($2->dump()); W($3->dump()); W(" -> "); W(module($2,$3).dump()); }
	| sym S			{ W($2->dump()); }
;
%%

