%{
#include "$MODULE.hpp"
%}
%defines
%union {
	char c;
	string *s;
	int n;
}
%token <c> CHAR SX
%token <s> VAR
%token <n> NUM
%%
$MODULE :
	| $MODULE CHAR		{ W(\$2); }
	| $MODULE SX VAR	{ W(var[*\$3]); }
	| $MODULE SX NUM	{ W(\$3); }
	| $MODULE SX		{ W(\$2); }
;
%%

